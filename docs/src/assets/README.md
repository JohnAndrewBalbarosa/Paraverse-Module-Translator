# `src/assets/`

This folder is the blueprint for the future asset-processing layer.

The files here should prepare binary assets and convert PDF-backed modules into translated slide output.

## Planned Files

- `assetResolver.js`: source-asset resolution, type detection, and caching
- `pdfToPptPipeline.js`: PDF extraction, structured translation, and PPTX generation
